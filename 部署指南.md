# 校园任务互助平台 - 部署指南

## 环境要求

- Node.js 16+ 或 Docker Desktop
- 现代浏览器（Chrome、Firefox、Edge 等）

---

## 方式一：本地部署（推荐先体验）

### 1. 安装 Node.js

下载并安装 Node.js：https://nodejs.org/

### 2. 启动后端服务

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 启动服务
npm start
```

后端服务将运行在：http://localhost:3000

### 3. 打开前端页面

直接用浏览器打开以下任意一个文件：
- `frontend/tasks.html` - 任务大厅（首页）
- `frontend/services.html` - 服务市场
- `frontend/forum.html` - 校园论坛
- `frontend/profile.html` - 个人中心
- `frontend/admin.html` - 管理后台

---

## 方式二：Docker 部署

### 1. 安装 Docker Desktop

下载并安装 Docker Desktop：https://www.docker.com/get-started

### 2. 启动服务

```bash
# 在项目根目录执行
docker compose up -d --build
```

### 3. 查看服务状态

```bash
# 查看容器状态
docker compose ps

# 查看后端日志
docker compose logs -f backend

# 查看 ngrok 日志（获取公网地址）
docker compose logs -f ngrok
```

### 4. 访问应用

- 前端：直接打开 `frontend/tasks.html`
- 后端 API：http://localhost:3000
- ngrok 管理界面：http://localhost:4040

### 5. 停止服务

```bash
docker compose down
```

---

## 配置 ngrok（可选）

1. 访问 https://ngrok.com/ 注册账号
2. 获取你的 authtoken
3. 复制 `.env.example` 为 `.env`
4. 在 `.env` 中填入你的 authtoken
5. 重启服务

---

## 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 测试用户1 | student1 | 123456 |
| 测试用户2 | student2 | 123456 |

---

## 项目结构

```
campus-task-platform/
├── backend/              # 后端服务
│   ├── server.js         # 主服务文件
│   ├── package.json      # 依赖配置
│   └── Dockerfile        # Docker 镜像配置
├── frontend/             # 前端页面
│   ├── tasks.html        # 任务大厅
│   ├── services.html     # 服务市场
│   ├── forum.html        # 校园论坛
│   ├── profile.html      # 个人中心
│   ├── admin.html        # 管理后台
│   ├── publish_task.html # 发布任务
│   ├── publish_service.html # 发布服务
│   ├── common.css        # 公共样式
│   └── common.js         # 公共脚本
├── docker-compose.yml    # Docker 编排配置
└── README.md            # 项目说明
```

---

## 常见问题

### 后端启动失败
- 检查 3000 端口是否被占用
- 确认 Node.js 版本 >= 16
- 查看控制台错误信息

### 前端无法连接后端
- 确认后端服务已启动（访问 http://localhost:3000/api/config/colors 测试）
- 检查浏览器控制台的错误信息
- 确认 common.js 中的 API_BASE_URL 配置正确

### 配色修改不生效
- 在管理后台修改配色后，刷新页面即可生效
- 检查浏览器控制台是否有错误

---

## 下一步

1. 本地测试完成后，可以：
   - 部署前端到 GitHub Pages
   - 使用 ngrok 提供公网访问
   - 配置数据库持久化数据

2. 生产环境部署建议：
   - 使用真实数据库（MongoDB、MySQL 等）
   - 添加用户认证
   - 配置 HTTPS
   - 添加日志和监控
